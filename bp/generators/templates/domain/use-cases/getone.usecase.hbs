import { getOne{{pascalCase collection_name}} } from '@/data-access/{{lowerCase collection_name}}.repo'
import { read{{pascalCase collection_name}}RequestParamSchema } from '@/domain/{{lowerCase collection_name}}/{{lowerCase collection_name}}.schema'

const getOne{{pascalCase collection_name}}UseCase = async (
  {{camelCase collection_name}}Id: typeof read{{pascalCase collection_name}}RequestParamSchema
) => {
  let data = await getOne{{pascalCase collection_name}}({{camelCase collection_name}}Id)
  return data
}

export default getOne{{pascalCase collection_name}}UseCase
